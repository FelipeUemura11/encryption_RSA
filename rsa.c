#include<stdio.h>

int main(void){

    int p, q, n, phi, e, d;

    printf("Informe um numero primo (p): ");
    scanf("%i", &p);

    printf("Informe um numero primeo (q): ");
    scanf("%i", &q);

    // Calcula n e phi(totiente de n)
    n = p * q;
    phi = (p -1) * (q -1);

    

}